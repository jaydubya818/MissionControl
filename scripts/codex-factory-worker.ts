#!/usr/bin/env tsx

/**
 * Retired in Runtime Contract v33.
 *
 * This worker called human Task actions while asserting an Agent identity. V1
 * Factory execution now runs through mission-control-orchestration and signed,
 * leased service commands.
 */

throw new Error(
  "scripts/codex-factory-worker.ts is retired. Start mission-control-orchestration instead.",
);
