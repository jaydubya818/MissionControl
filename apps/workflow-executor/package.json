{
  "name": "@mission-control/workflow-executor",
  "version": "0.9.0",
  "private": true,
  "description": "Retirement tombstone for the standalone workflow executor",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "pnpm run build && pnpm run start",
    "start": "node dist/index.js",
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.3.3"
  }
}
