{
  "name": "@mission-control/workflow-executor",
  "version": "0.9.0",
  "private": true,
  "description": "Standalone workflow executor for Mission Control",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mission-control/workflow-engine": "workspace:*",
    "convex": "^1.31.7",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
