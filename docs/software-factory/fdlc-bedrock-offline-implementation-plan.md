# Bedrock offline qualification implementation

Authority: Product Owner attachment 4b8efee4-c054-4a32-8221-ff510e409636,
2026-09-05. Sections 2 and 7 explicitly specify US geographic cross-region
inference. This is the current offline specification; the prior foundation-only
conflict remains historical evidence. No governed live route is issued.

Implement bounded non-streaming Converse and InvokeModel text/tool contracts,
strict US-profile/account/region policy, conservative versioned price conversion,
and a pre-send/settlement adapter over existing provider liability transitions.
Use injected transport and deterministic fixtures. No default credential chain,
network transport activation, registration or readiness is introduced.

Keep cache writes and extended reasoning disabled in WO1 requests; retain explicit
usage/pricing semantics and reject unsupported dimensions. Reserve full admitted
input capacity, never bytes/4. Retries remain zero; any attempted send retains its
hold on error, cancellation or unknown receipt. Reuse the canonical transactional
Convex ledger and regression evidence, not the legacy filesystem ledger.

The new APIs are internal modules. Existing newly planned provider-price API
accepts Converse/InvokeModel in addition to Responses; retain the seven additions
and v41-to-v42 lineage. No deployment/codegen/upload is needed for internal types.

Validation: adapter/route/pricing/budget/checker tests, actual Docker/recovery
regression, documentation, contract guard and System Qualification. Evidence is
OFFLINE / FIXTURE and cannot confer authority. Preserve the uncommitted worktree.
