# Recovered Governed GitHub App Proof

**Verification date:** 2026-08-09

The approved Mission establishes the intended outcome, and its approved Plan defines the bounded work. That Plan releases WorkOrder `kx7sm5meb0d1n9frm3v28aw15s8c7z3d`, which authorizes Task `hn7p4kczscg86e0k6bc8p9n94n8c6zxq`. Attempt `m97skgb9r5rgc013rgdkph1gv58c728q` executes only that Task in its isolated durable worktree.

The governed publication path is:

`Mission → approved Plan → WorkOrder → Task → Attempt → commit → pull request`

The commit must contain only `docs/software-factory/live-github-app-proof-recovery.md`. The governed publisher records the exact changed-file set and resulting commit SHA, then the bound GitHub App installation creates or updates the pull request and records its URL. The pull request remains unmerged so approval and merge authority stay outside the execution Attempt. No personal access token is permitted anywhere in this path.
