# Production execution admission blocker capability map

Recorded before implementation on 2026-08-21 from baseline
`95b6b2d18fb9f14b610d908338fb4e9d8054e171`.

| Blocker | Classification | Current primitive | Why it fails | Code gap | Environment gap | Minimum remediation |
| --- | --- | --- | --- | --- | --- | --- |
| GitHub App production installation | Production configuration + external operator action | `githubAppConnections.beginInstallation`, `/github/app/setup`, `bindExistingInstallation`, signed `/github/webhook`, and the orchestration GitHub App publisher | Production has no `githubAppInstallations` record and no GitHub App server variables. Readiness, Factory activation, Attempt claim, and publication therefore fail closed. | No new publication architecture is required. Existing readiness already checks exact App identity, repository access, least privilege, webhook subscriptions, and freshness. | Convex requires `GITHUB_APP_ID`, `GITHUB_APP_SLUG`, `GITHUB_APP_CLIENT_ID`, `GITHUB_APP_CLIENT_SECRET`, `GITHUB_APP_PRIVATE_KEY`, and `GITHUB_WEBHOOK_SECRET`. The worker requires the same App ID and private key. | An operator must configure the existing App secrets, set `/github/app/setup` as the setup callback, set `/github/webhook` as the signed webhook endpoint, install/authorize the App for the controlled repository, and complete canonical verification. No PAT fallback. |
| Exact model routes | Repository implementation, followed by production configuration | `modelCatalog.initializeDefaults` creates generic runtime aliases; `syncLocalModels` supports only local OLLAMA/LM Studio/MLX/VLLM discovery | Neither primitive can register `openai/gpt-5.6-terra` or the qualified OpenRouter route `openai/gpt-5.1-codex-mini`, nor freeze the Codex/capability/configuration/runtime identity. | Add a human-authorized, idempotent exact-route registration primitive with immutable identity, explicit enablement, separate evidence qualification/admission state, and attribution. Registration alone must remain non-qualifying. | Production catalog is empty. Exact qualified identities must be registered only after the implementation is deployed. | Add the minimum exact-route lifecycle and fail closed on identity drift, disabled state, missing qualification, or missing admission. |
| Hardened Sandbox Profile lifecycle | Repository implementation, followed by production configuration | `createSandboxProfile`, runtime `validateSandboxProfile`, and `sandboxProfileProductionEligible` | Runtime hardening correctly requires `security.qualificationOnly: true`; routing rejects that flag directly, leaving no legitimate human-reviewed transition. | Preserve the immutable qualification snapshot and add a separate, audited `QUALIFICATION_ONLY -> PRODUCTION_PILOT_ELIGIBLE` promotion bound to the exact profile digest and evidence. | Production has no Sandbox Profile. The qualified evidence packet must be referenced when a human promotes the exact created profile. | Add explicit promotion metadata and require it for production eligibility without changing `qualificationOnly`. Promotion grants execution eligibility only. |
| Certified profile representation | Repository implementation, followed by production configuration | `createSandboxProfile` stores a generic v1 snapshot | It always records `liveCertified: false`, cannot ingest the qualified security/toolchain/network/credential/workload scope, and incorrectly requires provider-enforced egress for a profile whose known limitation is guest nftables. | Accept and strictly validate the existing qualified security snapshot, policy digest, revocation bound, supported workload/risk scope, timeout constraints, and evidence identity. Preserve the provider-egress limitation explicitly. | The exact immutable image/evidence values must be supplied after deployment. | Extend the existing profile snapshot additively; keep historical profiles readable and never flatten provider and guest enforcement into one boolean. |
| Structured workflow status | Repository implementation; optional additive migration/read projection | `factoryWorkflowContractIssues` blocks unsafe definitions; legacy `workflows.upsert` can write global definitions without workspace authorization | All four active definitions lack a schema-required string `status`. The 13 project-scoped runs are historical records and cannot be rewritten honestly. | Add an explicit compatibility projection for legacy runs and a workspace-authorized production workflow registration path that requires the current contract. Existing unsafe definitions remain inadmissible for new Factory Versions. | A new current structured workflow must be registered after deployment. Existing records remain unchanged. | Classify each run from preserved source fields, expose normalized read lineage without inventing outcomes, and require current structured definitions for every new production run. |
| Exact Factory Versions | Production configuration | `factory/configuration.createVersion`, readiness assessment, activation | Creation is structurally available but all required production dependencies are absent; the model identity is only indirect through agent versions. | Freeze the selected exact model-route ID/digest in each Factory Version and include it in the configuration digest and readiness checks. | No definitions, code scopes, approved agents, policies, verifiers, GitHub App, workers, model routes, Sandbox Profile, or readiness records exist. | Deploy the lifecycle changes, configure canonical prerequisites, then create and assess the two exact versions. Version creation grants no execution authority. |
| Exact worker admission | Repository implementation + production configuration | `factoryWorkerEligibility` already checks adapter/version, manifest digest, effective config, backend, sandbox capabilities, and repository access | Provider/model are inferred from a primary agent, the exact catalog route is not frozen, and the worker report does not bind advertised model routes to capability/config identity. | Add an exact Factory-Version attestation containing configuration, route, harness, backend, Sandbox Profile, and repository identities; preserve all existing fail-closed checks. | No production worker binding exists. | Deploy the extension, create the exact Factory Versions, start a current canonical worker advertising their complete immutable identities, and require exact equality at readiness, dispatch, verification scheduling, and Attempt claim. |

## Production observations

- Admission records: zero GitHub App installations, model routes, Sandbox Profiles,
  workers, code scopes, verifiers, policies, Factory definitions/versions,
  readiness assessments, Attempts, routing policies, or routing decisions.
- Environment: only `VERCEL_AUTOMATION_BYPASS_SECRET` is configured in Convex
  production. No secret values were read or recorded.
- Workflows: four active definitions; every non-gate step fails
  `structured-status-required`.
- Workflow runs: 17 total, of which 13 are scoped to the production project.
  They are historical input to the compatibility audit, not current production
  Factory evidence.
- Guarded Auto remains disabled and no routing decision exists.
