# Qualification status: INVALID — interrupted

This attempt is retained as non-qualifying operational evidence. The composed
system qualification encountered a local `tsx` IPC socket `EPERM` error and the
process was interrupted. Although the partial `automated-checks.json` carries a
top-level `PASS`, it records only six of the required nineteen checks and must
not be treated as a completed or measured qualification result.

The scenarios, thresholds, and source SHA were not changed. The complete retry
is recorded in `context-skills-factory-admission-system-postmerge-v2`.
