# FDLC Bedrock live qualification evidence

State: `QUALIFICATION_AWS_INVOKE_PERMISSION_REQUIRED`.

The named SSO profile authenticated to the approved non-Production account. Safe
AWS responses prove the exact caller, ACTIVE US inference profile, three fixed US
destination model ARNs, and inference-profile-only Sonnet 4.6 model topology.

The cumulative liability ledger reserved `$0.924528` before the first synthetic
request. AWS rejected that request with HTTP 403 before inference because the
assigned Identity Center permission set still contains an explicit identity-based
`bedrock:InvokeModel` deny. The provider returned request ID
`24e8d1d5-c7ff-4cc3-b66d-32d08c4925ee`, one attempt, zero retry delay, no model
output, and no usage. The reservation is reconciled as
`REJECTED_PRE_INFERENCE`, actual model cost `$0`, leaving the full `$5` live
qualification ceiling available.

The exact required replacement policy is
`docs/software-factory/fdlc-bedrock-invocation-policy.json`. Only an Identity
Center administrator can apply and reprovision it; the sole assigned account role
cannot perform `sso:ListInstances`, `iam:GetRole`, or change its permission set.

`pricing-evidence.json` binds the conservative liability contract to current
primary AWS route documentation and Anthropic's 2026-05-27 US-only Sonnet 4.6
list prices. The pricing contract hashes that committed record directly.
