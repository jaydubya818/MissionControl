# Finite classification dispatch — isolated backend proof

The real loopback Convex backend passed 23 scenarios at 2026-09-06T07:08:21Z.
The new selected path issued exactly one persisted allowance under eight concurrent
claims; its canonical digest survived storage. Currentness negatives denied changed
revision, profile, worker, Factory configuration, qualification, price, request
parameters, budget and cancellation while retaining each allocation. The prior
13 identity/reconciliation/projection scenarios passed unchanged.

The gateway and shared constructors are unmodified production source, bound by
report hashes. The only authorization replacement is an explicit fixture-only
workspace permission shim. Selected profile, model route, Factory configuration
and execution manifest records use real canonical constructors and real currentness
checks. Related table schemas and approval records are synthetic. Inference tables
and indexes use the exact repository schema. This does not prove application auth,
live enrollment, provider usage, billing or real human acceptance.

Separate root code generation passed against another disposable loopback backend.
The generated API adds the extracted Attempt profile helper module; no public
function is added. Both backends stopped with their ports closed; original
services/databases and provider credentials were not used. Ephemeral admin keys
were not persisted. Harness files retain the qualification-host paths explicitly.

Development tests first reproduced 24 missing checks, then five independent-review
negatives. The final actual-handler/profile suite passed 91 tests, including 35
selected dispatch controls. Transport/wire qualification passed 97 tests.
Independent architecture/security/data review accepted all four corrected findings;
simplicity/docs review found no blocking issue. These results qualify the bounded
offline contract only. Full PR-head and clean-main gates are recorded separately.
