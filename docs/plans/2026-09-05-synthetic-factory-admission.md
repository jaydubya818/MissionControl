# Synthetic canonical Factory admission

Authorization: operator attachment b99e85b6-efff-4717-b4f6-340ec2542d35 explicitly permits source changes, qualification-only activation, canonical admission/dispatch, independent verification and browser proof. No production state, customer repository or external model execution is permitted. Qualification is not an authorization bypass.

Reconciled baseline: origin/main f90c50f (full SHA recorded by Git), runtime contract 42. The earlier uncommitted checkpoint is retained in a named Git stash and immutable evidence. Preserve main's Context7 currentness and local-candidate recovery controls.

Public contract changes, one increment to 43: existing isolated sandbox registration; additive offline profile/workflow registration and worker report unions; explicit qualification target/evidence on existing Factory activation. Schema additions bind qualification activation to exact existing environment, workspace, repository, version/configuration/profile, actor, readiness receipt and expiry. No alternate Task/Attempt, admission engine or evidence table.

Offline Factory activation requires a dev environment explicitly configured on the deployment as the isolated qualification environment. Freeze and revalidate its scope and identity. A production target, missing deployment binding, stale evidence or changed version must fail. This restriction never grants authorization: canonical workspace approval permission and all applicable readiness/currentness gates remain mandatory.

Complete canonical WorkOrder admission using the exact deterministic contract and zero provider authority. Reserve the entire resource ceiling in the existing Attempt authorization, transactionally accounting for prior WorkOrder, Mission and policy reservations. Retain unknown costs. Revalidate live authority on claim, renewal and result. Preserve independent Git candidate verification and human publication gates.

Qualification: synthetic persisted Mission/approved Plan/WorkOrder, exact profile and Factory, real worker and independent verifier; lifecycle negative controls; actual browser desktop/390px/keyboard/refresh/accessibility; current System Qualification and full repository gates; independent security, data integrity, architecture, simplicity and documentation reviews. Rebuild exact artifacts from captured current source and fix CLI substitution and durable evidence replay concerns before claiming completion.

Only after the complete synthetic flow passes: focused local commits, immutable evidence, and evaluate existing remote-landing authorization without production side effects. Do not consume the historic twelve-call approval.

## Completion

The frozen `context-skills-synthetic-factory/v1` scenario completed on run `v13`. The producer and verifier used separate Attempts, Factory Versions, Execution Profiles, worktrees, leases, and invocation identities. The exact unpublished candidate received a server-derived `VERIFIED` result. Synthetic evidence remains excluded from production acceptance and publication authority. Browser refresh, desktop/mobile rendering, accessibility, failure controls, zero provider calls, and temporary-permission removal are recorded in `docs/testing/evidence/context-skills-factory-admission-v1/`.
