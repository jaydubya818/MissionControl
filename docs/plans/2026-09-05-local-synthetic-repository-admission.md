# Local synthetic repository admission

Authorization: attachment aa63cc95-4940-4264-ab20-616403dc4fbb. Implement a first-class LOCAL_SYNTHETIC_QUALIFICATION repository capability, never an exemption on GitHub admission. Existing unpublished handoff invariants remain.

The control plane binds a versioned admission to the already established synthetic tenant, project/engagement and operator, an explicit development environment, fixture, canonical disposable root, baseline/tree and host. Deployment-owned configuration fixes these identities; registration accepts no caller-supplied identity/path. The worker must independently attest actual root ownership, bounded fixture content, no remote, no symlinks and exact Git baseline. Freeze the admission digest into Factory composition and execution authority. A repository record alone cannot authorize execution.

Use canonical readiness/admission/Task/Attempt/candidate/verification records. GitHub checks remain unchanged for GITHUB repositories. The separate local readiness policy reports GitHub/publication requirements NOT_APPLICABLE_FOR_LOCAL_SYNTHETIC_QUALIFICATION and requires its own admission proof plus every common gate. Deny publication, PR, release and production at authoritative entry points and worker defense boundaries.

Implement and test exact scope/path/baseline/runtime/currentness negatives, then run actual producer and independent verifier through the canonical backend. Human acceptance remains separate and is not authorized by repository admission. Browser proof must use persisted state and honest local qualification labels. Preserve every failure and historical evidence. No external model calls or production changes. No completion claim before full proof and independent reviews.
