{
  "name": "@mission-control/telegram-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "V1 tombstone for the unauthenticated Telegram runtime",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "pnpm run build && pnpm run start",
    "dev:bj": "pnpm run build && pnpm run start:bj",
    "start": "node dist/index.js",
    "start:bj": "node dist/bj-bot.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
