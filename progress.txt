# Mission Control -- Progress Tracker
# Read this file at the start of every session to understand current state.
# Update this file at the end of every session with what was done.

## Current Version: 0.9.0

## Current Phase: Phase 2 (Agent Runtime) COMPLETE → Phase 3 Ready

## Last Session: July 27, 2026

### Docs site + EOS live adapters + Pi bridge slice

1. **Tessl-style docs site** — `docs/site/` (22 guides), in-app Documentation tab with sidebar browser, internal markdown link routing, semantic search indexes `docs/site/`.
2. **Demo seed mc-demo-v2** — `convex/lib/demoSeedExtensions.ts`: 9 registry packages, eval runs, knowledge graph, work orders, goals, harness PR checks, meta-loop suggestions, ~104 alerts; enables demo flags including `executor.pi-bridge`.
3. **EOS live projections** — `convex/eos/projections.ts` wired into Factory Health, Effectiveness, Friction, Readiness, Recommendations, Missions, Command Center (demo fallback when sparse).
4. **Pi receipt ingestion** — `factory/piBridge:ingestReceiptPacket`, orchestration `POST /workorders/:id/receipt-packets`, `mc-context scan` for org skill inventory.
5. **Nav badges** — Tasks, Execution, Incidents, Registry, Memory counts from `schematicOverview`; harness routes in E2E smoke.

### Harness engineering — live wiring + wizard E2E

1. **Adoption metrics (Convex)** — `factory.health.getAdoptionMetrics`: human touches/agent task, shared component contributions, workflow vs interactive token spend.
2. **Merge gates (live)** — `factory.prChecks.getMergeGateStatus` + `lib/mergeGates.ts`; Architect Mode + Change Review use real PR check data.
3. **Code review wizard E2E** — 7-step wizard: evidence from PR/CI/QC, editable findings, verifier from skill, Change Risk policy, meta loop schedule.
4. **Meta loop → eval lineage** — Accept `EVAL_SCENARIO` creates `contextEvalScenarios` with PR lineage in description.
5. **Conference pages wired** — AI Patterns + Architect Mode metrics from Convex; Factory Health shows new KPIs.
6. **Registry** — Outer-loop unified eval/verifier/security summary; Evals tab shows `eval.framework` flag banner.
7. **Harness extras** — Harness-first PR comment modal, Control Plane mutation panel, Work Ledger hazard banners.

### Conference UI pages (Harness nav)

- AI Patterns, Architect Mode, Software Factory (ADW), Workshop, Agent Fleet, Automations — all in sidebar under Harness.

## Last Session (archived): July 11, 2026

#### Memory Knowledge Graph v1 + EOS merge + demo ergonomics
1. **Knowledge graph** — Convex schema/API, Graphify import, Memory → Graph tab.
2. **Sprint 1 exception-first Overview** — AttentionQueuePanel wired into DashboardOverview and EOS CommandCenterView.
3. **EOS in main repo** — AppShellV2 flag swap, convex/analytics.ts.
4. **Demo from main** — `pnpm run dev:demo` / `dev:demo:ui`.
