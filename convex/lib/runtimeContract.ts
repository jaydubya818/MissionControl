/**
 * Increment when a deployed client and backend cannot safely operate together.
 * The compatibility query name must remain stable across contract versions.
 */
export const RUNTIME_CONTRACT_VERSION = 42;
