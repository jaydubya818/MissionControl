{
  "name": "mission-control",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "description": "Mission Control for OpenClaw - Autonomous agent orchestration and observability",
  "author": "Jarrett West",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "pnpm --filter @mission-control/orchestration-server dev",
    "dev": "concurrently \"npx convex dev\" \"pnpm --filter mission-control-ui dev\"",
    "dev:ui": "pnpm --filter mission-control-ui dev",
    "dev:demo": "concurrently -n convex,executor,ui -c blue,magenta,green \"npx convex dev\" \"pnpm run dev:workflow-executor\" \"VITE_FLAG_UI_SHELL_V2=true VITE_FLAG_CONTEXT_REGISTRY=true VITE_FLAG_EOS_COMMAND_CENTER_PREVIEW=true pnpm --filter mission-control-ui exec vite --port 5199 --strictPort\"",
    "dev:demo:ui": "VITE_FLAG_UI_SHELL_V2=true VITE_FLAG_CONTEXT_REGISTRY=true VITE_FLAG_EOS_COMMAND_CENTER_PREVIEW=true pnpm --filter mission-control-ui exec vite --port 5199 --strictPort",
    "dev:workflow-executor": "pnpm --filter @mission-control/workflow-engine build && pnpm run workflows:seed && node --env-file=.env.local --import tsx apps/workflow-executor/src/index.ts",
    "dev:orchestration": "pnpm --filter @mission-control/orchestration-server dev",
    "build": "pnpm -r --if-present run build",
    "build:workspaces": "pnpm -r --filter './packages/*' build",
    "test": "pnpm -r --if-present run test && pnpm exec vitest run convex/__tests__",
    "test:integration": "npx vitest run",
    "test:e2e:arm": "npx playwright test -c playwright.config.ts tests/e2e/arm-ui.e2e.spec.ts",
    "test:e2e:critical": "npx playwright test -c playwright.config.ts tests/e2e/critical-accessibility.e2e.spec.ts tests/e2e/dashboard-smoke.e2e.spec.ts tests/e2e/v2-routes-smoke.e2e.spec.ts",
    "lint": "pnpm run typecheck && node scripts/skill-lint.mjs",
    "typecheck": "pnpm run ci:prepare && pnpm -r --if-present run typecheck",
    "clean": "turbo run clean && rm -rf node_modules",
    "setup": "pnpm install && pnpm run build",
    "convex:dev": "npx convex dev",
    "convex:push": "bash scripts/convex-push-ts-only.sh",
    "convex:deploy": "npx convex deploy",
    "convex:seed": "npx convex run seed:seedV0",
    "convex:seed:demo": "npx convex run seedMissionControlDemo:run",
    "convex:seed:demo:force": "npx convex run seedMissionControlDemo:run '{\"force\": true}'",
    "convex:seed:demo:full": "npx convex run seedMissionControlDemo:run '{\"force\": true}' && npx convex run seedMemory:run",
    "convex:seed:hiring": "npx convex run seedAgentHiring:run",
    "import:knowledge-graph": "node scripts/import-knowledge-graph.mjs",
    "import:knowledge-graph:demo": "node scripts/import-knowledge-graph.mjs --project-slug sf-demo",
    "migration:health": "npx convex run migrations/backfillInstanceRefs:getMigrationHealth",
    "migration:backfill:refs": "npx convex run migrations/backfillInstanceRefs:runBackfill",
    "migration:backfill:tenant": "npx convex run migrations/backfillInstanceRefs:runTenantBackfill",
    "workflows:seed": "node --env-file=.env.local --import tsx scripts/seed-workflows.ts",
    "agent:run": "pnpm --filter @mission-control/orchestration-server dev",
    "ci:prepare": "pnpm --filter @mission-control/shared build && pnpm --filter @mission-control/state-machine build && pnpm --filter @mission-control/policy-engine build && pnpm --filter @mission-control/workflow-engine build && pnpm --filter @mission-control/coordinator build && pnpm --filter @mission-control/context-router build && pnpm --filter @mission-control/model-router build && pnpm --filter @mission-control/memory build && pnpm --filter @mission-control/context-tools build && pnpm --filter @mission-control/agent-runtime build && pnpm --filter @mission-control/telegraph build && pnpm --filter @mission-control/meetings build",
    "ci:typecheck": "pnpm run typecheck",
    "ci:test": "pnpm run test",
    "ci:test:e2e": "pnpm run test:e2e:arm",
    "ci:lint": "pnpm run lint",
    "ci:runtime-contract": "node scripts/check-runtime-contract.mjs",
    "taskmaster:plan": "taskmaster plan docs/PRD_V2.md",
    "taskmaster": "taskmaster"
  },
  "dependencies": {
    "convex": "^1.42.3"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.50.0",
    "@taskmasterai/cli": "^1.0.55",
    "concurrently": "^8.2.2",
    "tsx": "^4.7.0",
    "turbo": "^1.11.2",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
